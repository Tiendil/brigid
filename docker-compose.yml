version: "3.9"


services:

  brigid:
    build:
      context: ./
      dockerfile: ./docker/Dockerfile

    volumes:
      - ${PWD}/:/repository
      - ${PWD}/.env:/repository/.env

    # environment:

    ports:
      - "8000:8000"
